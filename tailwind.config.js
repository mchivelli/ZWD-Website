// Tailwind CSS v4.0 uses CSS-first configuration
// All theme customization should be done in src/index.css using @theme {}
// This file is kept minimal for compatibility but configuration is in CSS

export default {
  // v4.0 handles content detection automatically
  // Configuration moved to CSS @theme {} block
}
